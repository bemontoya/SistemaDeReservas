# 1. Configuración de la base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/reserva_db
spring.datasource.username=root
spring.datasource.password=C4dpeafg**
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 2. Hibernate solo debe validar, no crear nada
spring.jpa.hibernate.ddl-auto=validate

# 3. Activación del Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# 4. Puerto (Debe ser distinto ya que cada microservicio tiene su propio puerto)
server.port=8084